import FuturesConfigLayout from "@/components/futures-config-layout"

export default function ConfigLayout({ children }: { children: React.ReactNode }) {
  return <FuturesConfigLayout>{children}</FuturesConfigLayout>
}
